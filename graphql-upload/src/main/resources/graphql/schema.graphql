scalar Upload

type Query {
  _: Boolean
}

type Mutation {
  uploadFile(bucket: String!, fileName: String!, folder: String!, file: Upload!): Boolean!
  deleteFile(bucket: String!, fileName: String!, folder: String!): Boolean!
}
